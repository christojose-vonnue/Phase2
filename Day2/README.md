# Description

A file-based Task Manager CLI, that can add, list, complete, delete and filter tasks.

# Commands

* `task add`        : To add a new Task
* `task delete `    : To delete a new Task
* `task complete`   : To set status as complete
* `task filter`     : To filter tasks by status
* `task list`       : To list out status

